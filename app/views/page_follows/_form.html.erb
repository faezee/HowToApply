<%= form_for(@page_follow) do |f| %>
  <% if @page_follow.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page_follow.errors.count, "error") %> prohibited this page_follow from being saved:</h2>

      <ul>
      <% @page_follow.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :folowee_type %><br>
    <%= f.text_field :folowee_type %>
  </div>
  <div class="field">
    <%= f.label :folowee_id %><br>
    <%= f.number_field :folowee_id %>
  </div>
  <div class="field">
    <%= f.label :folower_type %><br>
    <%= f.text_field :folower_type %>
  </div>
  <div class="field">
    <%= f.label :folower_id %><br>
    <%= f.number_field :folower_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
