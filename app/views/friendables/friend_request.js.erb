alert("salamss");
<% if @f.blank? %>
$("form#request input[type=submit]").attr('value', 'unfollow');
$("form#request input[type=submit]").removeClass("btn-success").addClass("btn-danger")
<% else %>
$("form#request input[type=submit]").attr('value', 'follow');
$("form#request input[type=submit]").removeClass("btn-danger").addClass("btn-success")
<% end %>